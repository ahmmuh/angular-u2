export class User {
  constructor(
    public username: string,
    public name: string,
    public city: string,
    public street: string,
    public zipcode: number
  ) {}
}

// username, name, city, street, zipcode)
